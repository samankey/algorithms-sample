import { describe, expect, it } from "vitest";
import { solve } from "./solution";

describe("programmers 12906 같은-숫자는-싫어", () => {
  it("sample 1", () => {
    const input = "";
    const out = solve(input);
    expect(out.trim()).toBe("");
  });
});
